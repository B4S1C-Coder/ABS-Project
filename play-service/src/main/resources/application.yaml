spring:
  application:
    name: play-service
  cloud:
    aws:
      credentials:
        access-key: test
        secret-key: test
      region:
        static: us-east-1
      ssm:
        endpoint: http://localhost:4566
      s3:
        endpoint: http://localhost:4566

app:
  cdn:
    enabled: true
    base-url: http://localhost:8080
  buckets:
    streams: streams-bucket
